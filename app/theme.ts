"use client";

import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#072AC8",
    },
    secondary: {
      main: "#0077b6",
    },
  },
});

export default theme;
